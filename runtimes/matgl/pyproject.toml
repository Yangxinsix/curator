[project]
name = "curator-runtime-matgl"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "curator-torch",
  "matgl>=4.0",
  "matscipy",
]

[tool.uv.sources]
curator-torch = { path = "../..", editable = true }
