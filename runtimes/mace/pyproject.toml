[project]
name = "curator-runtime-mace"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "curator-torch",
  "mace-torch",
  "matscipy",
]

[tool.uv.sources]
curator-torch = { path = "../..", editable = true }
