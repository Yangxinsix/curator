[project]
name = "curator-runtime-nequip"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "curator-torch",
  "nequip",
  "nequip-allegro",
  "matscipy",
]

[tool.uv.sources]
curator-torch = { path = "../..", editable = true }
