[project]
name = "curator-runtime-sevennet"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "curator-torch",
  "sevenn",
  "matscipy",
]

[tool.uv.sources]
curator-torch = { path = "../..", editable = true }
