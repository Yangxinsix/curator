[project]
name = "curator-runtime-mattersim"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "curator-torch",
  "mattersim",
  "matscipy",
]

[tool.uv.sources]
curator-torch = { path = "../..", editable = true }
