defaults:
  - calculator: vasp                        # use VASP calculator settings

_target_: curator.label.annotator.AtomsAnnotator   # annotator that runs calculator on structures
inspector: null                                    # optional inspector to validate results
