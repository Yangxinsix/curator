method: ensemble                # use ensemble-based uncertainty estimation
threshold: forces_sd            # metric name to threshold on
threshold_value: 0.05           # lower uncertainty threshold for accepting samples
threshold_maxvalue: 0.5         # upper cap to mark as highly uncertain
