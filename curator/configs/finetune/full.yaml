# @package _global_

# Standard full-parameter fine-tuning mode:
# - set `model_path.mode: weights` when you want checkpoint weight loading
# - keep all parameter groups trainable unless overridden elsewhere
finetune: full
