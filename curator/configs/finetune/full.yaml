# @package _global_

# Standard full-parameter fine-tuning mode:
# - load checkpoint weights into the current architecture
# - keep all parameter groups trainable unless overridden elsewhere
finetune: full

task:
  checkpoint_mode: weights
