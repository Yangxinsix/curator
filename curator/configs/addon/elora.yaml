# @package _global_

defaults:
  - _self_
  - override /finetune: elora    # reuse existing LoRA冻结/优化器策略

addon:
  name: elora
  use_cueq: false
  use_elora: true
  wrapper_stack: elora
  elora_rank: 16
  elora_alpha: 16.0
  elora_freeze_base: true
  description: Enable ELoRA adapters on e3nn backend; no cueq dependency.

task:
  checkpoint_mode: weights
