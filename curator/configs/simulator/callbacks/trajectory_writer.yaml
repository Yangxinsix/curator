_target_: curator.simulate.callbacks.trajectory.TrajectoryWriter  # writes trajectories during MD
path: ${outputs.pool_set}                                         # output path for the default single-system hand-off
interval: 1                                                       # write every N steps
per_system: false                                                 # default workflow hand-off writes a single trajectory
