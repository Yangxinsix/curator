# @package _global_
model:
  output_modules:
    node_feature_projection:
      _target_: curator.layer.FeatureProjection
      in_features: ${model.representation.num_features}
      out_features: ${task.teacher_node_features}
task:
  teacher_node_features: 224
