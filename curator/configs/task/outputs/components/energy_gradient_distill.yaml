_target_: curator.train.ModelOutput
name: energy_gradient_distill
prediction_property: energy_gradient_forces
target_property: forces
loss_fn:
  _target_: curator.train.VectorL2MAELoss
loss_weight: ${oc.select:task.loss_weights.energy_gradient_distill,0.0}
only_train: true
