_target_: curator.layer.PairwiseDistance   # processes precomputed edge geometry during the model forward
compute_neighbor_list: False               # expect the dataloader to provide neighbor lists
compute_distance_from_R: False             # reuse provided edge vectors if available
cutoff: ${data.cutoff}                     # radial cutoff used when upstream data computes neighbors
